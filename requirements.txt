# Core requirements — needed from Sprint 0 onward.
# Install with: pip install -r requirements.txt
#
# Versions are PINNED (==), not floors (>=). The floors that used to be here
# resolved to whatever was newest at install time, so the metrics in README.md
# were not reproducible from this file: the environment they were measured in
# ran pandas 3.0.5 and numpy 2.4.6, both a major version above the old floors.
# Only direct dependencies are pinned -- see AUDIT.md (P1-2) for why this is
# not a full `pip freeze` lockfile.

# Data handling & classical ML (Sprints 1-2)
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
joblib==1.5.3

# Plotting / EDA (Sprint 1)
matplotlib==3.11.1

# Agent / CLI interface (Sprint 6)
rich==14.3.4

# Testing (all sprints)
pytest==9.1.1
